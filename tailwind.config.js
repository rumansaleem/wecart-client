module.exports = {
  theme: {
    // Some useful comment
    container: {
      center: true,
      padding: '1rem',
    },

    extend: {
      maxHeight: {
        'half-screen': '50vh',
      },
      height: {
        current: '1em',
      },
    },
  },
  variants: {
    // Some useful comment
  },
  plugins: [
    // Some useful comment
  ],
}
